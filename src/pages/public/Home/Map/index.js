import React from "react";
import classNames from "classnames/bind";
import styles from "./Map.module.scss";

const cx = classNames.bind(styles);

export default function Map() {
  return <div>This is map right side</div>;
}
